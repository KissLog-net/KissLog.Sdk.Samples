@model KissLog.Samples.WebApi.Models.IndexViewModel

<div>
    <p>This ASP.NET WebApi application is using KissLog to save the logs on:</p>

    <ol>
        <li>
            KissLog.net: <br />
            <a href="@Model.KissLogRequestLogsUrl" target="_blank">@Model.KissLogRequestLogsUrl</a>
        </li>
        <li class="mb-3">
            Local text files: <br />
            @Model.LocalTextFilesPath
        </li>
    </ol>
</div>

@section css {
    <style>
        .body-content { padding-top: 60px; }
        ol > li { margin-bottom: 20px; }
    </style>
}
